# homebrew-tap

Homebrew tap for [rackctl](https://github.com/rackctl/rackctl) CLIs.

```sh
brew install rackctl/tap/rackctl
```

Formulae here are published automatically by goreleaser on each `rackctl` release.
